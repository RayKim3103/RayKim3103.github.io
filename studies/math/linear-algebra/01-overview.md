---
layout: page
title: "01. 선형대수 개요"
permalink: /studies/math/linear-algebra/01-overview/
sitemap: false
---

- **원본 노트**: [GitHub](https://github.com/RayKim3103/Undergraduate-Course/blob/main/%5BUndergraduate%5D_Linear_Algebra/lecture_notes/01%20%EC%84%A0%ED%98%95%EB%8C%80%EC%88%98%20%EA%B0%9C%EC%9A%94.md)

{% raw %}
## 한눈에 보기

이 장은 전기전자공학에서 선형대수를 왜 배우는지, 그리고 앞으로 다룰 핵심 대상이 무엇인지 소개한다. 강의의 중심 메시지는 선형대수가 거의 모든 공학 시스템을 행렬과 벡터로 표현하고 해석하는 언어라는 점이다.

선형대수의 기본 단위는 다음처럼 이어진다.

```text
튜플 또는 벡터 -> 선형 함수 -> 행렬 -> 선형 시스템 -> 공학 문제 해석
```

특히 신호처리, 무선통신, MIMO 시스템, 최적화, 인공지능, 네트워크, 제어 등은 모두 벡터와 행렬을 통해 문제를 세운 뒤 계산한다.

## 강의 운영

- 강의는 대면 수업으로 진행된다.
- 강의 자료는 LearnUs에 업로드된다.
- 질문은 수업 중 또는 이메일로 가능하다.
- 영어가 주된 강의 언어이며, 학생은 한국어와 영어 모두 사용할 수 있다.
- 과제와 퀴즈는 선형대수 이해에 중요하므로 여러 세트가 제공된다.
- 토론과 협업은 권장되지만, 제출물은 독립적으로 작성해야 한다.

## 교재와 학습 방향

주 교재는 Gilbert Strang의 *Linear Algebra and Its Applications*이다. 다만 강의 슬라이드가 핵심 내용을 담고 있으므로 수업 자료 중심으로 공부해도 된다. 슬라이드는 MIT와 Stanford의 선형대수 강의 흐름을 바탕으로 구성되어 있다.

이 강의에서 기대하는 학습은 단순 계산이 아니라 다음 과정 전체를 익히는 것이다.

- 공학 현상을 수학적으로 모델링한다.
- 문제를 선형 방정식 또는 행렬 문제로 정식화한다.
- 행렬과 벡터의 구조를 이용해 효율적인 풀이 전략을 세운다.
- 해의 존재성, 유일성, 차원, 구조를 해석한다.

## 왜 전기전자공학에서 선형대수가 중요한가

전기전자공학에서 선형대수는 다른 수학 과목과 여러 전공 과목을 연결하는 중심축이다.

관련 분야는 다음과 같다.

- 신호 및 시스템
- 디지털 신호처리
- 디지털 통신
- MIMO 통신
- 정보이론
- 무선 시스템 프로토타이핑
- 확률과 통계적 추론
- 추정 이론
- 최적화
- 강화학습과 머신러닝
- 압축 센싱
- 네트워크와 제어

공학 시스템을 분석할 때 입력, 출력, 채널, 잡음, 상태, 파라미터를 모두 벡터나 행렬로 묶을 수 있다. 따라서 선형대수는 공학 문제를 계산 가능한 형태로 바꾸는 기본 언어가 된다.

## 선형대수의 중심 질문

선형대수는 한 문장으로 말하면 선형 시스템을 다루는 학문이다.

선형 시스템은 보통 다음 형태로 나타난다.

$$
Ax = b
$$

여기서 각 기호의 의미는 다음과 같다.

- $A$: 시스템 또는 변환을 나타내는 행렬
- $x$: 미지수 벡터 또는 입력 벡터
- $b$: 출력, 관측값, 목표값, 우변 벡터

따라서 선형대수의 목표는 행렬을 능숙하게 다루는 것이다. 단순히 역행렬을 계산하는 데서 끝나지 않고, 행렬이 어떤 공간을 만들고 어떤 정보를 잃거나 보존하는지 이해해야 한다.

## 공학 문제의 선형 방정식 예시

### 교통 흐름

도로 구간의 유입량과 유출량을 미지수로 두면 보존 법칙에 의해 선형 방정식이 생긴다.

예를 들어 교차로마다 들어오는 교통량과 나가는 교통량이 같아야 하므로 다음과 같은 식이 나온다.

$$
w + 120 = x + 250
$$

$$
x + 120 = y + 70
$$

$$
y + 530 = z + 390
$$

$$
z + 115 = w + 175
$$

이 문제는 교통량 미지수 $w, x, y, z$를 구하는 선형 시스템으로 볼 수 있다.

### 화학 반응식 균형

화학식의 계수를 맞추는 문제도 원자 수 보존 조건을 세우면 선형 방정식이 된다.

예시 식은 다음처럼 표현된다.

$$
2x = z
$$

$$
6x = 2w
$$

$$
2y = 2z + w
$$

각 원소별 개수 보존이 행렬 방정식의 행이 된다.

### MIMO 무선통신 시스템

전기전자공학에서 특히 중요한 예시는 MIMO 통신이다. 송신 안테나 $i$에서 전송한 심볼을 $x_i$라 하고, 수신 신호를 $y_j$라 하면 다음과 같은 선형 결합으로 표현할 수 있다.

$$
y_1 = h_{11}x_1 + h_{12}x_2 + \cdots + h_{1n}x_n + w_1
$$

$$
y_n = h_{n1}x_1 + h_{n2}x_2 + \cdots + h_{nn}x_n + w_n
$$

행렬 형태로는 다음과 같다.

$$
y = Hx + w
$$

여기서 $H$는 채널 행렬이고, $w$는 잡음 벡터이다. 수신기는 $y$와 $H$를 이용해 $x$를 추정해야 하므로, 이는 선형대수 문제이다.

## 튜플과 벡터

튜플은 순서가 있는 원소들의 목록이다.

$$
(x_1, x_2), \quad (x_1, x_2, \ldots, x_n)
$$

공학적으로 튜플은 신호 샘플, 상태 변수, 특징 벡터, 안테나별 송신 심볼 등을 표현할 수 있다.

튜플에 대해 정의되는 연산은 크기와 구조가 맞아야 한다.

### 덧셈

같은 차원의 튜플끼리는 성분별로 더한다.

$$
\begin{bmatrix}
x \\
y
\end{bmatrix}
+
\begin{bmatrix}
w \\
z
\end{bmatrix}
=
\begin{bmatrix}
x+w \\
y+z
\end{bmatrix}
$$

크기가 다른 튜플 사이의 덧셈은 정의되지 않는다. 예를 들어 2차원 벡터와 3차원 벡터는 바로 더할 수 없다.

### 스칼라 곱

수 $c$와 벡터의 곱은 각 성분에 $c$를 곱한다.

$$
c
\begin{bmatrix}
x \\
y
\end{bmatrix}
=
\begin{bmatrix}
cx \\
cy
\end{bmatrix}
$$

## 선형 함수

함수 $f$가 선형 함수가 되려면 다음 두 조건을 만족해야 한다.

### 덧셈 보존

$$
f(x+y) = f(x) + f(y)
$$

### 스칼라 곱 보존

$$
f(cx) = cf(x)
$$

이 두 조건은 선형 함수가 벡터 공간의 구조를 보존한다는 뜻이다.

스칼라 입력의 경우 선형 함수는 일반적으로 다음 형태이다.

$$
f(x) = ax
$$

상수항이 붙은 함수

$$
f(x) = ax + b
$$

는 $b \neq 0$이면 선형이 아니다. 이는 $f(0)=0$ 조건을 만족하지 않기 때문이다.

## 튜플에서의 선형 함수 예시

다음 함수는 2차원 벡터를 스칼라로 보내는 선형 함수이다.

$$
f
\left(
\begin{bmatrix}
x \\
y
\end{bmatrix}
\right)
= x + 2y
$$

이는 행렬 곱으로 다음처럼 표현할 수 있다.

$$
f
\left(
\begin{bmatrix}
x \\
y
\end{bmatrix}
\right)
=
\begin{bmatrix}
1 & 2
\end{bmatrix}
\begin{bmatrix}
x \\
y
\end{bmatrix}
$$

또 다른 예시는 2차원 벡터를 2차원 벡터로 보내는 함수이다.

$$
f
\left(
\begin{bmatrix}
x \\
y
\end{bmatrix}
\right)
=
\begin{bmatrix}
x+y \\
3x
\end{bmatrix}
$$

이 함수도 행렬로 표현된다.

$$
f
\left(
\begin{bmatrix}
x \\
y
\end{bmatrix}
\right)
=
\begin{bmatrix}
1 & 1 \\
3 & 0
\end{bmatrix}
\begin{bmatrix}
x \\
y
\end{bmatrix}
$$

따라서 선형 함수는 행렬 곱으로 구현되고, 행렬은 선형 시스템의 본체가 된다.

## 핵심 정리

- 선형대수는 선형 시스템 $Ax=b$를 이해하는 학문이다.
- 벡터는 공학 변수와 신호를 묶어 표현하는 기본 단위이다.
- 행렬은 시스템, 변환, 채널, 관계식을 표현한다.
- 선형 함수는 덧셈과 스칼라 곱을 보존한다.
- 전기전자공학의 많은 문제는 선형 방정식, 행렬 곱, 행렬 분해, 역문제로 환원된다.

## 연결되는 노트

- [행렬과 가우스 소거](02-matrices-and-gaussian-elimination.md)
- [벡터공간과 네 가지 기본 부분공간](03-vector-spaces-and-four-subspaces.md)
- [선형변환](04-linear-transformations.md)
- [직교성](05-orthogonality.md)

## 복습 체크리스트

- [ ] 선형 시스템 $Ax=b$에서 $A$, $x$, $b$의 의미를 설명할 수 있다.
- [ ] 튜플의 덧셈과 스칼라 곱이 언제 정의되는지 말할 수 있다.
- [ ] 선형 함수의 두 조건을 쓸 수 있다.
- [ ] 상수항이 있는 함수가 왜 일반적으로 선형이 아닌지 설명할 수 있다.
- [ ] MIMO 시스템을 행렬 방정식으로 해석할 수 있다.

## 보강 학습 노트

### 큰 그림

- 이 문서는 **01. 선형대수 개요**를 다루며, 벡터, 행렬, 선형변환, 부분공간, 직교성을 통해 공학 문제를 구조적으로 푸는 언어를 익힌다.
- 단편적인 정의를 외우기보다 입력이 무엇이고, 내부에서 어떤 변환이 일어나며, 출력이나 성능 지표가 어떻게 결정되는지 흐름으로 잡는 것이 좋다.
- 앞뒤 단원과 연결해 보면 이 주제가 왜 필요한지, 어떤 가정을 추가하거나 완화하는지 더 분명해진다.

### 핵심을 더 깊게 보기

- 행렬은 숫자 표가 아니라 선형변환이며, rank와 null space는 정보가 보존되거나 사라지는 방향을 말한다.
- Gaussian elimination은 해를 구하는 절차이면서 column space, pivot, independence를 드러내는 도구다.
- orthogonality와 projection은 least squares, signal decomposition, PCA 같은 과목으로 이어진다.

### 문제 풀이 또는 구현 루틴

- 문제를 matrix equation으로 쓰고 unknown vector의 차원과 equation 수를 확인한다.
- row reduction 후 pivot columns, free variables, null space basis를 따로 기록한다.
- 기하학적으로는 span, projection, transformation이 벡터를 어떻게 움직이는지 그림으로 해석한다.
- 마지막에는 단위, 차원, boundary condition, edge case를 확인해 계산 결과가 현실적인지 검산한다.

### 자주 하는 실수

- row space와 column space는 서로 다른 ambient space에 있을 수 있다.
- invertible 여부는 square matrix에서만 말하며 rank 조건과 연결된다.
- orthogonal과 independent는 관련이 있지만 같은 개념이 아니다.
- 정의를 그대로 적용하기 전에 이 단원에서 전제한 ideal assumption이 실제 문제에서도 유지되는지 확인한다.

### 스스로 점검할 질문

- 이 행렬이 보존하는 방향과 없애는 방향은 무엇인가?
- 해가 없거나 무한히 많을 조건을 rank로 설명할 수 있는가?
- projection error가 왜 column space에 직교해야 하는가?
- **01. 선형대수 개요**를 한 문장으로 설명하고, 관련 수식이나 회로/알고리즘/시스템 그림 없이도 핵심 흐름을 말할 수 있는가?

{% endraw %}

---

다음: [02. 행렬과 가우스 소거](02-matrices-and-gaussian-elimination.md)
